/**
 * @file use-animated-state.js
 * @module svelte-motion/animation/use-animated-state
 * Re-exports the {@link module:svelte-motion/animation/UseAnimatedState.svelte~UseAnimatedState} component.
 * This structure allows for a cleaner import path for users, typically `import { UseAnimatedState } from 'svelte-motion/animation';`
 * or as part of the main library export `import { UseAnimatedState } from 'svelte-motion';`.
 *
 * @example
 * ```javascript
 * // Importing from the specific animation module
 * import { UseAnimatedState } from 'svelte-motion/animation';
 *
 * // Or, if exported from the main library index:
 * // import { UseAnimatedState } from 'svelte-motion';
 * ```
 */
export {default as UseAnimatedState} from './UseAnimatedState.svelte';